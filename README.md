# homebrew-agent-plans

Homebrew tap for installing `agent-plans` cask.

## Install

```bash
brew tap tanabe1478/agent-plans
brew install --cask agent-plans
```

Or install without tapping first:

```bash
brew install --cask tanabe1478/agent-plans/agent-plans
```
